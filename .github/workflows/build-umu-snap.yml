name: UMU Snap Build
on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  release:
    types: [published]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: 🧪 Build snap on amd64
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: |
          sudo apt-get install cargo python3-hatch-vcs

      - name: 🧪 Build snap on amd64
        uses: snapcrafters/ci/test-snap-build@5d324dfd09955a81a018be6d7544d07770552644
        with:
          snapcraft-project-root: ./packaging/snap

      - name: Upload artifact
        uses: actions/upload-artifact@v4.6.0
        with:
          name: Snap
          path: ./packaging/snap/*.snap
